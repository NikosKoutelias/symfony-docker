{
    // ${workspaceFolder} is the current folder the VScode has opened
    "version": "0.2.0",
    "configurations": [
    
        {
            "name": "Launch for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9003,
            "pathMappings": {
                "/var/www/html": "${workspaceFolder}/symfony"
            },
            "log": true

        }

    ]
}

